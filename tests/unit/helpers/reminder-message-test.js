import {
  reminderMessage
} from 'koopman-mobile/helpers/reminder-message';

module('ReminderMessageHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = reminderMessage(42);
  ok(result);
});
