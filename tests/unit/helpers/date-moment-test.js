import {
  dateMoment
} from 'koopman-mobile/helpers/date-moment';

module('DateMomentHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateMoment(42);
  ok(result);
});
